# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TravelAgency' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAgency
  pod 'Alamofire', '~> 4.8.0'
  pod 'SwiftyJSON', '~> 4.0'
  
end

target 'TravelAgency WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAgency WatchKit App
  pod 'Alamofire', '~> 4.8.0'
  pod 'SwiftyJSON', '~> 4.0'

end

target 'TravelAgency WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TravelAgency WatchKit Extension
  pod 'Alamofire', '~> 4.8.0'
  pod 'SwiftyJSON', '~> 4.0'

end
